

/**
 * this package works as a data layer between the incoming xml response,
 * and between my service. It converts, or map the xml doc instance to JAXB element object based on my schema,
 * so can easily map the xml to java objects and populate them easily. 
 * 
 * @author Idrees Al-hashemi
 * */
package dataType.jaxb;
